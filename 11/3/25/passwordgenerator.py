import random
import string

password=""
char = "abcdefghijklmnopqrstuv"

#make sure variable has the empt"""
for i in range(7):
    #random.choice=random.choice (char)

    password += (random.choice)
print(f"password generated:{password}")